{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "lint": "lerna run lint",
    "format": "lerna run format",
    "test": "lerna run test --scope=@nito/web"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
